import React from 'react';
import Navbar from '../components/Navbar'; // adjust the path if needed

const Home: React.FC = () => {
  return (
    <div>
      <Navbar />
    </div>
  );
};

export default Home;
